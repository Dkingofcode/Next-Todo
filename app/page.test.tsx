// import React from 'react';
// import { render, screen, fireEvent } from '@testing-library/react';
// import Home from './page';  // Import your component


// test('renders todo app correctly', () => {
//     render(<Home />);  // Render your component

//     // You can write assertions to test if your component renders as expected
//     // For example:
//     expect(screen.getByText('TODO')).toBeInTheDocument();
//     expect(screen.getByPlaceholderText('Create a new todo...')).toBeInTheDocument();
// });

// test('adds a new todo', () => {
//     render(<Home />);

//     const input = screen.getByPlaceholderText('Create a new todo...');
//     const addButton = screen.getByText('Add');

//     // Simulate typing a new todo and clicking the add button
//     fireEvent.change(input, { target: { value: 'New Todo' } });
//     fireEvent.click(addButton);

//     // Check if the new todo is added
//     expect(screen.getByText('New Todo')).toBeInTheDocument();
// });


//  // Write more tests for other functionality like filtering, removing, 




















